<% @messages.each do |message| %>
<h2><%= message.title %></h2>
<%= link_to "Ver mensajes", message_path(message), class: "btn btn-default" %>

<% end %>


<%= link_to "Nuevo Mensaje", new_message_path  %>
